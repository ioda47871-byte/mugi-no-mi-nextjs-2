# 実装・内容・収益化・公開の状態

最終更新: 2026-09-01（Cloudflare Pages公開手順と正式名称を決定）

現時点の正確な表現は **「サイト基盤完成、実データの投入が進行中」** です。
計画書の Task 5（約30商品・10記事）は目標に近づきましたが未達、Task 8 の実リンク・
本番確認は**未完了**です。

| 区分 | 状態 |
|---|---|
| 実装完了 | **完了** |
| 内容準備完了 | **一部完了**（実商品 23 件／目標30件、公開記事 7 本／目標10本） |
| 収益化設定完了 | **一部完了**（楽天の目視照合済み・表示可能リンク 14 件、未照合で非表示 1 件。公開商品 7 件は販売先リンク未登録。Amazonは未設定） |
| 本番公開完了 | **未完了**（Cloudflare Pages と `tabimono-hikaku.jp` は選定済みだが、ドメイン取得・Cloudflare設定・デプロイ・公開用運営者情報が未完了） |

---

## 1. 実データの件数

### 本番データセット（`datasets/production/`）

| 区分 | 件数 |
|---|---:|
| 商品（登録） | **23 件** |
| 商品（公開） | **22 件** |
| 商品（review・非公開） | 1 件 |
| 出典 | 24 件（うち編集確認済み 23 件） |
| 根拠付きの仕様値 | 158 件 |
| 公開記事 | **7 本** |
| 下書き記事 | 3 本 |
| 販売先リンク（登録） | **15 件**（楽天） |
| 販売先リンク（照合済み・目視確認済み・表示可能） | **14 件**（楽天） |
| 販売先リンク（未照合・非表示） | 1 件（楽天） |
| 販売先リンク未登録の公開商品 | 7 件 |

### カテゴリ別の内訳

| カテゴリ | 公開 | ブランド |
|---|---:|---|
| スーツケース | 9 件 | エース 5 / プロテカ 2 / ワールドトラベラー 2 |
| 旅行用リュック | 5 件 | ace. GENE LABEL 4 / エレコム 1 |
| 収納ポーチ | 4 件 | ace. TOKYO LABEL 3 / エレコム 1 |
| モバイルバッテリー | 4 件 | Anker 4（ほかにエレコム1件を review で保留） |

### 取り込みの経路

| 経路 | 件数 | 内容 |
|---|---:|---|
| `direct-fetch`（自分で取得） | 20 件 | 2026-08-31 にメーカー公式ページを直接取得。エース系16件・Anker 4件 |
| `provided-document`（提供資料） | 4 件 | エレコムの3商品と回収告知一覧。エレコム公式は 403 で取得できないため資料のまま |

エース クレスタ2 06936 は、提供資料で取り込んでいた値と同じページを直接取得して
一致を確認できたため、出典を `direct-fetch` に更新しました（数値は変更なし）。

### 公開している22件に共通して足りないもの

| 項目 | 状態 |
|---|---|
| 販売先の紹介URL | 14件は目視照合済みで表示可能。1件は未照合のため非表示。公開商品7件は販売先リンク未登録です |
| Amazon | 全商品未設定（`AMAZON_ASSOCIATE_TAG` も未設定） |
| 定格電力量(Wh) | モバイルバッテリー4件とも公表なし。null のまま |
| 重量 | ポーチ（フォルケット）3件は公表なし。null のまま |
| 寸法の測定条件 | エース系リュック・ポーチ、Anker全件で公表なし（`unspecified`） |

review で保留している エレコム DE-C63-10000BK は、メーカーの回収・リコール告知の
最新確認が終わっていないため、引き続き非公開です。

---

## 2. 出典の入手経路

**取り込み済みの仕様値は、どれも各メーカーの公式ページへ自分で接続して取ったものではありません。**
取り込み当時の環境では外部ドメインへの HTTPS が遮断されていたため、資料の提供を受けました。

> 2026-08-31 の点検で、実行環境によっては外部へ到達できることが分かりました（「4. 購入ボタン（CTA）の
> 確認状況」の「外部への到達性が変わりました」を参照）。ただし**取り込み済みデータの入手経路は
> 書き換えません。** 自分で取得し直した値だけを `direct-fetch` として記録します。

これを取り違えないよう、`Source` に入手経路を機械可読で記録しています。

| フィールド | 値 | 意味 |
|---|---|---|
| `provenance` | `provided-document` | 別環境で調査された資料からの取り込み |
| `checkedAt` | `2026-08-31` | **資料に記載された「公式ページを確認した日」** |
| `importedFrom.document` | `datasets/production/research-materials/…` | 根拠資料の保管場所 |
| `importedFrom.importedAt` | `2026-08-31` | **このリポジトリへ取り込んだ作業日** |
| `automatedFetch` | `unverified` | 自動取得の可否は未確認（資料の提供は許可ではない） |
| `llmInput` | `unverified` | 原文を外部AIへ渡す権限は未確認 |

検証で次を強制しています。

- `provided-document` なのに `importedFrom` が無ければエラー
- `direct-fetch` なのに `importedFrom` があればエラー（自力確認と偽れない）
- 取込日が未来ならエラー

画像は権利確認ができていないため、**4件とも `image: null`**（文字主体のカード）です。

---

## 3. 寸法の測定条件

「ハンドルを除く寸法」を「ハンドル込み外寸」の欄へ入れないため、条件欄を追加しました。

- `sizeBasis`: `with-handle-and-wheels` / `body-only` / `excludes-handle` / `excludes-handle-and-straps` / `unspecified`
- `measurementState`: `not-applicable` / `normal` / `expanded` / `compressed`
- `alternateMeasurements[]`: 拡張時など別条件の寸法・容量

画面には条件込みの見出しで出ます。実際の表示：

| 商品 | 画面上の見出し | 値 |
|---|---|---|
| エース 06936 | 外寸（ハンドル・キャスター含む） | 350 × 550 × 250 mm |
| エース 06936 | 本体寸法（本体のみ） | 340 × 480 × 250 mm |
| エレコム ポーチ | 拡張時の外寸（ハンドルを除く） | 240 × 340 × 140 mm |
| エレコム ポーチ | 拡張時の容量 | 12 L |
| エレコム リュック | 通常時の外寸（ハンドル・ショルダーベルトを除く） | 320 × 510 × 200 mm |
| エレコム リュック | 拡張時の外寸（ハンドル・ショルダーベルトを除く） | 320 × 510 × 250 mm |
| エレコム バッテリー | 外寸（測定条件の公表なし） | 64 × 91 × 24 mm |

比較表でも条件ごとに行を分けているため、条件の違う値が同じ行に並ぶことはありません。

---

## 4. 購入ボタン（CTA）の確認状況

| 項目 | 状態 |
|---|---|
| 4状態の画面確認（両方／楽天のみ／Amazonのみ／どちらもなし） | **完了**（テスト専用データ・外部通信遮断） |
| カテゴリ画面・記事画面でのボタン表示 | **完了**（テスト専用URLでの通し確認） |
| href が登録した紹介URLと完全一致すること | **完了**（クエリ追加・中継なしを確認） |
| クリック計測の送信内容 | **完了**（`affiliate_click` の中身を確認） |
| 計測失敗時にリンクが機能すること | **完了** |
| 実際の紹介URLの登録 | **完了**（2026-08-31 受領・登録） |
| 実リンクでのボタン表示・遷移先・計測 | **完了** |
| リンク先を開いての商品・サイズ・色の目視確認 | **完了**（2026-08-31 運営者が確認） |

### 通し確認のやり方（`npm run test:e2e:linkcheck`）

1. `datasets/production` を `.preview/linkcheck-dataset` へ複製する。
2. 複製側のポーチの楽天リンクだけを、**テスト専用の架空URL**で `verified` にする。
3. `CATALOG_DATASET_DIR` でその複製を読ませてビルドし、e2e を実行する。

**本番データセットは変更していません**（`status: "unverified"` / `affiliateUrl: null` のまま）。
`.preview/` は git 管理外です。`SITE_MODE=production` では `CATALOG_DATASET_DIR` を
使用できず（ビルド時に例外）、`check:release` も設定されていれば失敗します。

### 確認できた内容

| 確認項目 | 結果 |
|---|---|
| カテゴリ画面のボタン | 楽天ボタンが1つ。紹介ID未設定のAmazonボタンは出ない |
| 記事画面のボタン | 同じ商品カードに同じボタンが1つ |
| 遷移先 | 登録した紹介URLと**完全一致**。クエリ文字列の追加なし、中継ページなし、HTTPS、許可ホストのみ |
| リンク属性 | `rel="nofollow sponsored noopener"` / `target="_blank"` / `noreferrer` は付けない / 新規タブである旨の読み上げ用テキスト |
| 広告表示 | ページ上部の広告表示と、ボタン直下の「広告リンクです」 |
| クリック計測（カテゴリから） | `{article_slug:"", category_id:"pouches", product_id:"elecom-bma-trcs01mbk-m-black", merchant:"rakuten", placement:"category-card"}` |
| クリック計測（記事から） | `{article_slug:"pouch-size-weight-compartments", category_id:"", product_id:"…", merchant:"rakuten", placement:"article-card"}` |
| 送信内容の安全性 | URL・クエリ文字列・メールアドレスを含まない。キーは識別子5つのみ |
| 計測失敗時 | `gtag` が例外を投げてもクリックは通常どおり進む（遷移を妨げない） |

計測は外部通信を遮断した状態で、ページ自身の `gtag` シムが `dataLayer` に積んだ内容を
読んで確認しました。**Google へは何も送信していません。**

`npm run preview:cta` が `.preview/cta/index.html` を生成します。
このページは **本番の静的出力（`out/`）には含まれません**。使用している ASIN・紹介URL・
トラッキングIDはすべてテスト専用の架空値で、本番データセットには書き込んでいません。

`tests/e2e/cta-preview.spec.ts` が、`file://` 以外の通信をすべて遮断したうえで、
ボタン数・rel属性・`noreferrer` が無いこと・target・ホスト・クリック領域の高さ・フォーカス・
スマートフォンでの縦積みを検証します。

### 初回に有効化した楽天リンク（2026-08-31、現在は14件へ拡大）

| 項目 | 内容 |
|---|---|
| 商品 | エレコム BMA-TRCS01MBK（Mサイズ / ブラック） |
| 販売先 | 楽天市場 `nanshindo:j197439` |
| 紹介URL | 管理画面で発行されたものを**加工せずそのまま**登録（クエリの追加・削除なし） |
| 状態 | `verified`（判断根拠: **リンク先を目視確認**） |

確認できたこと:

- カテゴリ画面・記事画面に楽天ボタンが1つずつ出る
- `href` が発行された紹介URLと**完全一致**（ビルド成果物で照合）
- `rel="nofollow sponsored noopener"`、`target="_blank"`
- クリック計測が意図した内容を送る（カテゴリ／記事で識別子を出し分け）
- 販売先の**価格・在庫・画像を取り込んでいない**（3,186円という表示も、楽天配信の商品画像も出力に無い）
- リンク未発行の商品（スーツケース・リュック）にはCTAもダミーリンクも出ない

リンク先の商品・サイズ（M）・色（ブラック）は、2026-08-31 に運営者が実際に開いて
確認済みです。同日の点検では当方からもリンク先を取得でき、型番・JAN・サイズ・色・店舗の
一致を確認しました（下記「外部への到達性が変わりました」）。データ上の根拠は、より強い
運営者の目視確認（`visual`）のままにしています。

### 「照合済み」の判断根拠を記録するようにしました

自動取得を入れたことで、`verified` に2種類の意味が混ざるようになりました。
両者を混同しないよう、`MerchantLink.verificationMethod` に根拠を必ず記録します。

| 値 | 意味 |
|---|---|
| `visual` | 人がリンク先を開いて商品・サイズ・色を確認した |
| `identifier-match` | 型番・JANの一致で判断した（リンク先は開いていない） |

- `status: verified` で根拠が無いデータは**検証エラー**になります。
- 自動取得ジョブが作るリンクは**必ず** `identifier-match` です。目視確認にはなりません。
- `npm run link:set --verify` は `--visual-check` か `--identifier-match` の指定が必須です。
- `validate:content` が内訳を出します（現在: 照合済み 14 件／目視確認 14・型番一致のみ 0）。

### rel から noreferrer を外した件

楽天が発行するリンクコードが `rel="nofollow sponsored noopener"` で `noreferrer` を
含んでいなかったため、こちらも合わせました。リファラを落とすと成果の判定に
影響する可能性があるためです。タブナビング対策は `noopener` だけで足ります。
成果判定の実際の仕組みは各社の非公開情報のため確認できていません。
あわせて、遷移時に当サイトのページURLが販売先へ伝わることをプライバシーページに明記しました。

### 実導線の確認に残っていること

現在、楽天の14件は目視照合済みで購入ボタンを表示できます。未照合のクレスタS 09162は
非表示のままです。**公開中の22商品のうち7商品には販売先リンクが未登録**のため、
購入ボタンを表示せず収益につながりません。

| カテゴリ | 表示できる（照合済み） | 登録済みだが未照合 | 未発行 |
|---|---:|---:|---:|
| スーツケース | 5 | 1 | 3 |
| 旅行用リュック | 4 | 0 | 1 |
| 収納ポーチ | 4 | 0 | 0 |
| モバイルバッテリー | 1 | 0 | 3 |

2026-09-01 に13件を確認して表示対象にしました（既存1件と合わせて14件）。
確認はクラウドブラウザで販売ページを開いて行い、型番・容量・指定色の選択肢・
初期選択状態・販売状態を確かめています。

**未照合のまま残しているのは クレスタS 09162（60/73L）1件です。** 指定色の
ガンメタリック（02）は商品ページに存在しますが「再入荷」表示で現在購入できない
ため、購入ボタンを出しません。

### 選択式の販売ページについて

確認した10件は、指定色を含む**複数色の選択式ページ**でした。いずれも初期状態は
「未選択」で、別色が自動で選ばれることはありません。誤った色へ直接誘導する形では
ないものの、**購入者自身が対象の色・サイズを選ぶ必要があります。**
そのため購入導線の注記に次を追加しました。

> 色・サイズが選択式の場合は、販売ページで対象の仕様を選択してください。

個別の状態も note に残しています。クレスタ2 06936 は別色のネイビーヘアラインが
再入荷表示、プロテカ ソリエ3 12871 は別色のパープルワインが売り切れですが、
いずれも対象色には影響しません。

自動取得から外した4件（ラディアル06971・06972／DE-C63／BM-BPTRCSEPBK）は、
「当店限定色の疑い」「リコール確認が未了」「法人向け通販で一般購入の可否が不明」
のため保留にしています。

**紹介URLの発行は運営者の作業です。** 楽天アフィリエイトの管理画面で商品を検索し、
発行されたURLを `npm run link:set` で登録します（手順は下記）。
アプリIDを設定すれば `npm run rakuten:sync` で自動取得もできますが、
自動取得で付く根拠は `identifier-match`（型番一致）であって目視確認ではありません。

### 外部への到達性が変わりました（2026-08-31 の点検で判明）

**これまで「外部ドメインへ接続できない」と記録していましたが、今回の実行環境からは
一部の販売先・メーカーへ到達できました。** 環境ごとに変わるため、断定せず都度確認します。

| ドメイン | 今回の結果 |
|---|---|
| `item.rakuten.co.jp` | 到達（200） |
| `hb.afl.rakuten.co.jp` | 到達（ルートは 404。紹介URLの形でのみ意味を持つ） |
| `www.ace-dot.com`（エース） | 到達（200） |
| `www.elecom.co.jp`（エレコム） | 403（サイト側で拒否。内容は取得できず） |
| `openapi.rakuten.co.jp`（楽天API・現行版） | 到達（資格情報なしでは 400。applicationId と accessKey が必要） |

この到達性を使い、登録済みの楽天リンクの**リンク先を実際に取得して照合しました**。

| 照合項目 | 結果 |
|---|---|
| 店舗 | 南信堂 楽天市場店（`nanshindo`）— 登録値と一致 |
| 商品名 | 「旅行用パッキングキューブ Mサイズ BMA-TRCS01MBK 《 エレコム 》」 |
| 型番・JAN | `BMA-TRCS01MBK` / `4549550317535` をページ内に確認 |
| サイズ・色 | M / ブラック（型番末尾 MBK と本文表記が一致） |
| 販売終了・在庫切れの表示 | 検出なし |

これは運営者の目視確認（2026-08-31）を**裏づける**ものです。データ側の
`verificationMethod` は `visual` のまま変更していません（人の確認の方が強い根拠のため）。

**ただし、既に取り込み済みの仕様値の入手経路（`provenance`）は書き換えません。**
提供資料から取り込んだ事実は変わらないためです。今後あらためて自分で取得できた値だけを
`direct-fetch` として記録します。

紹介URLの発行時に使える検索語（公開中で販売先リンク未登録の7商品）:

| カテゴリ | 商品 | 検索語 |
|---|---|---|
| スーツケース | エース ラディアル 06971 / 06972 | `エース ラディアル 06971` / `06972` |
| スーツケース | プロテカ 360G4 02420 | `プロテカ 360G4 02420` |
| リュック | エレコム BM-BPTRCSEPBK | `BM-BPTRCSEPBK` |
| モバイルバッテリー | Anker A110DN11 / A110EN11 / A110MN12 / A1335011 | 型番、または `Anker Zolo Power Bank 10000mAh` など |

**色・サイズまで一致する商品ページを選んでください。** 当サイトの登録は
「35L / 01 ブラックヘアライン」のようにバリエーションまで指定してあり、
バリエーションが一致しないリンクは検証で弾かれます。

### 受け取った紹介URLの登録方法

```bash
CATALOG_DATASET=production npm run link:set -- \
  --product elecom-bma-trcs01mbk-m-black --merchant rakuten \
  --url "<管理画面で発行した紹介URL>" --verify
```

- ホストが `hb.afl.rakuten.co.jp` / `a.r10.to` 以外なら**登録を拒否**します
  （`item.rakuten.co.jp` の通常商品URLは拒否されることを確認済み）。
- `matchedVariant` は商品の `variant` を自動で使うため、不一致で非表示になることがありません。
- 書き込み前にカタログ全体の検証を通します。`--dry-run` で内容だけ確認できます。
- `--verify` を付けなければ `unverified` のまま登録します（画面には出ません）。

---

## 5. 読者向け画面と内部情報の分離

以下を読者向けページから外し、プレビュー限定の「開発情報」（既定で折りたたみ）と
このドキュメントへ移しました。

- 「現在の掲載状況」「登録済み出典○件」「データセット」の表示
- 環境変数名（`SITE_URL` / `PUBLIC_OPERATOR_NAME` など）
- 「仮称です」の注記
- 商品カードごとの「紹介ID未設定」「型番照合が未完了」といった運営側の事情

一方、次は読者に必要な情報として**残しています**。

- 広告・アフィリエイトリンクを含む旨の表示
- 出典リンクと仕様の確認日（「仕様の詳細」内）
- 実際に使用した体験ではない旨の説明
- 「不明」の意味と、推定値を入れていないこと

商品カードは、商品名 → 本体重量 → 容量または定格電力量 → 用途に関わる機能 →
主な注意点 → 購入導線 の順に変更し、全仕様は「仕様の詳細を見る」に折りたたみました。

プレビューの上部表示は、デモデータ使用中のみ「デモデータ表示中（すべて架空）」、
実データのときは「未公開プレビューです」に切り替わります。

### スマートフォンの固定ヘッダー

ご指摘の重なりは、**実際のスクロールでは再現しませんでした**（長いページ全体を1枚に
収めるスクリーンショットの撮影仕様によるものです）。360px幅で scrollY を 0/300/600/900 と
変えて計測し、フィルタ見出し・重量セレクト・件数表示のいずれもヘッダー帯に
食い込んでいないことを確認しています。回帰確認として e2e にも追加しました。
現時点でバグとしての修正は行っていません。

---

## 6. 実行した検証コマンドと結果

以下は2026-08-31時点の履歴です。現在のCloudflare公開リハーサルでは、実行環境に
Node.js 22 と Chromium が無いためブラウザE2Eを0件実行しています。`test:e2e`、
`test:e2e:production`、`test:e2e:linkcheck` の3系統は本番前にすべて成功が必須です。

| コマンド | 結果 |
|---|---|
| `npm run typecheck` | 成功 |
| `npm run lint` | 成功 |
| `npm test` | **114 件成功**（catalog 34 / rakuten 34 / affiliate 17 / publication 15 / audit 14） |
| `npm run validate:content:all` | production・demo とも検証OK |
| `npm run build:only`（デモ） | 成功・25ページ |
| `SITE_MODE=preview CATALOG_DATASET=production npm run build` | 成功（Preview相当の環境変数） |
| `npm run test:e2e`（デモ + CTA） | **50 件成功** |
| `npm run test:e2e:production`（実データ + CTA） | **36 件成功** |
| `npm run test:e2e:linkcheck`（購入導線の通し確認） | **34 件成功** |
| `CATALOG_DATASET=production npm run audit` | 確認事項は「購入リンク未発行の公開商品」のみ。期限切れ・不整合なし |
| `SITE_MODE=preview CATALOG_DATASET=production npm run check:release -- --out out` | 期待どおり非ゼロ終了（未達3件＝SITE_MODE・公開情報・noindex。いずれも運営者の環境変数設定待ち）。配信物に架空データ・テスト値・秘密情報の混入なし |

プレビュー相当のビルド成果物で、次を直接確認しました。

- `robots.txt` が `Disallow: /`、`sitemap.xml` の URL 数が 0
- 全ページ上部が「未公開プレビューです」（「デモデータ表示中」は出ない）
- `/categories/pouches/` の楽天ボタンが 1 件、`rel="nofollow sponsored noopener"`

E2E をこの実装環境で動かす場合は、`PW_CHROMIUM_PATH=/opt/pw-browsers/chromium` のように
インストール済み Chromium のパスを渡してください（Playwright の自動ダウンロードは行えません）。

---

## 7. 収益化設定：未達成

| 項目 | 状態 | 必要な操作 |
|---|---|---|
| 楽天の紹介URL | **14件が目視照合済み・表示可能**。1件は未照合で非表示、公開22商品中7商品は販売先リンク未登録 | 7件は管理画面で紹介URLを発行 → `npm run link:set` で登録。検索語の一覧は4節にあります |
| 楽天APIの自動取得 | 接続確認済み（2026-08-31、dry-run）。認証成功・紹介URL取得・型番/JAN照合まで確認。書き込みは未実行 | 利用する場合だけ、資格情報3つをGitHub Actions Repository Secrets、`RAKUTEN_API_REFERER` をActions Variablesへ設定する。Cloudflare Pagesには設定しない。次は `--mode links` の dry-run → 内容確認 → `--apply`。手順は docs/rakuten-automation.md |
| Amazon アソシエイトID | 未提供 | `AMAZON_ASSOCIATE_TAG` を設定（空の間はAmazonボタンを出力しません） |
| Amazon 審査 | 未確認 | 登録後180日以内に3件以上の適格販売、10件以上のオリジナル投稿。条件達成は合格の保証ではありません |
| 計測ID (GA4) | 未提供 | `NEXT_PUBLIC_GA_ID`。未設定でも計測タグは出力されません |
| Search Console | 未設定 | 本番URL確定後に登録 |

---

## 8. 本番公開：未達成

| 項目 | 状態 |
|---|---|
| 正式名称 | 決定: 旅モノ比較 |
| 公開候補ドメイン | tabimono-hikaku.jp（2026-09-01 JPRS WHOISで登録情報なし。購入時に再確認） |
| 本番ホスティング | 決定: Cloudflare Pages Free / GitHub連携（未設定・未デプロイ） |
| 本番公開 | 未完了（`tabimono-hikaku.jp` は未取得、Cloudflare設定・公開用運営者名・連絡先が必要） |
| 公開用の運営者名・連絡先 | 未提供（架空の値は入れず、画面には「準備中」と表示） |
| 本番URL / HTTPS / canonical / robots / sitemap の実サイト確認 | 未実施 |

現在は `SITE_MODE=preview`（全ページ noindex、robots.txt は全面 Disallow）です。

---

## 9. 今回やっていないこと

- **目標30商品までの残り7件**：今回は23件まで取り込みました。エレコム公式が 403 で
  取得できないなど、メーカーによって到達性が違うため、機械的に埋めることはできません。
- **追加記事3本**：10本の目標に対して7本を公開しました。残る3本
  （2〜3泊用リュックの選び方／2泊3日の持ち物リスト／洗面ポーチの選び方）は、
  それぞれ必要な商品・情報がまだ足りないため下書きのままにしています。
  洗面ポーチは該当する商品を1件も取り込めていません。
- **販売先リンクの拡充**：紹介URLの発行は運営者の作業のため、当方では進められません。
- **Phase 2 の着手**：楽天API取得、AI記事生成、自動公開、定期実行、価格・在庫表示は
  すべて未実装・OFF のままです（`AUTOMATION_ENABLED=false`、`LLM_MONTHLY_BUDGET_USD=0`）。

---

## 10. 費用について

Phase 1 の実行時 LLM API 費用は 0 円です（閲覧時に外部APIを呼びません）。

ただし、**ドメインとホスティングだけで今後の費用が済むとは限りません。**
Phase 2 で AI/API による記事案生成や定期取得ジョブを採用する場合、
API利用料と実行基盤（GitHub Actions の実行時間など）の費用が別途発生し得ます。
これらは未承認・未契約であり、着手前に対象機能と金額を確認します。

---

## 11. 設計上の判断メモ

| 判断 | 理由 |
|---|---|
| `sizeBasis` / `measurementState` / `alternateMeasurements` を追加 | 「ハンドルを除く」「拡張時」の値を無条件の外寸として格納しないため |
| `Source.provenance` / `importedFrom` を追加 | 提供資料からの取り込みを「自分で接続して確認した」と記録させないため |
| `jan` を追加 | 販売先の照合と楽天での商品検索に使うため（架空値は入れない） |
| バッテリーを `review` で保持 | 安全告知の追加確認が済むまで公開しない。仕様データの取り込みは進める |
| CTA確認を `.preview/` へ出力 | 本番の静的出力に混ぜないため。Playwright が `test-results/` を消すため別ディレクトリ |
| e2e をデータセット別に分離 | デモは絞り込み・0件・下書き非公開を厚く、本番データは寸法条件と件数を確認 |
