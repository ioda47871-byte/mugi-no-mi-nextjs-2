{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "{ [ \"$VERCEL_ENV\" = \"production\" ] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; } && exit 1; git diff --quiet HEAD^ HEAD -- . || exit 1; exit 0"
}
